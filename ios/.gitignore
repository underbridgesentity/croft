App/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# Generated Config files are COMMITTED (regenerated by `npx cap sync`) - the
# Xcode Cloud runner can't run cap sync (no network for npm), so the checkout
# must already contain them. config.xml is in the App target's Resources phase.
# App/App/capacitor.config.json is committed too.
